#include <stdio.h>
#include <stdlib.h>

int check_result(int a, int b, char exercise) {
if( a == b )
    printf("correcto, a: %d == b:%d", exercise, a, b);
else
    printf("a) incorrecto, a: %d, a_comp:%d", a, a_comp);

}

int main() {
    /* a) */
    int a_comp = 0b10000000000000000000000000000000;
    int a = 1 << 31;

    /* b) */
    i

    return 0;
}